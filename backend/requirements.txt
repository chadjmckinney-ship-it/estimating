fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pydantic-settings>=2.0.0
email-validator>=2.0.0
# The proposal workbook (sql/080). Missing until the Fedora deploy of 2026-09-09 found it.
openpyxl>=3.1
# Time zones for the bid list's due hours (sql/082); Windows has no zoneinfo of its own.
tzdata>=2024.1
# The box's Outlook sender (~/daily-status-report/outlook_mail.py) for backend/email_concrete_orders.py.
msal>=1.28
requests>=2.31
